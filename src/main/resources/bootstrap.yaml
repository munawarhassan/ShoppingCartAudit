spring: 
  cloud:
    config:
      uri: ${currencyservice.configserverurl:http://localhost:8888}
      fail-fast: false   
  application:
    name: shoppingcartaudit
  profiles:
    active: dev

server:
  port: 8094
  